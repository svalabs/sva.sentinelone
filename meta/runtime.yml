---
requires_ansible: '>=2.16.0'
plugin_routing:
  modules:
    sentinelone_agent_info:
      redirect: sva.sentinelone.agent_info
    sentinelone_config_overrides:
      redirect: sva.sentinelone.config_overrides
    sentinelone_download_agent:
      redirect: sva.sentinelone.download_agent
    sentinelone_filters:
      redirect: sva.sentinelone.filters
    sentinelone_groups:
      redirect: sva.sentinelone.groups
    sentinelone_path_exclusions:
      redirect: sva.sentinelone.path_exclusions
    sentinelone_policies:
      redirect: sva.sentinelone.policies
    sentinelone_sites:
      redirect: sva.sentinelone.sites
    sentinelone_upgrade_policies:
      redirect: sva.sentinelone.upgrade_policies
